# 配置模板：复制为 .env 并填入真实值（.env 已被 .gitignore 排除，不会进仓库）。也可以用 api/config.js 配置文件方式，见 api/config.example.js，配置文件优先于环境变量
# 部署到 Vercel 时在项目 Settings → Environment Variables 配置同名变量；
# 自行部署时把这两个变量配给运行 api/notify-feishu.js 的 Node 进程。

# 飞书群机器人 webhook 地址（预约提醒推送）
FEISHU_WEBHOOK_URL=

# 预约转发接口地址（preBook，由合作方提供）
PREBOOK_API_URL=
